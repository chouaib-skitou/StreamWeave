# Issue Templates

Issue templates live under `docs/issues/` during the repository bootstrap and should be mirrored into `.github/ISSUE_TEMPLATE/` when GitHub automation is added.

Every issue should identify the outcome, context, acceptance criteria, affected service or contract, failure cases, observability needs, test plan, and out-of-scope behavior.
