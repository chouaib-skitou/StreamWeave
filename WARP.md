# Workspace Notes

This file contains public workspace-level notes for contributors. The private agent instructions and BMAD installation remain in local ignored files.

Use the documentation map in `README.md` to find the current source of truth. When a design decision affects service boundaries, contracts, persistence, reliability, or security, record it in the appropriate architecture decision before implementation.
