apiVersion: 1
providers:
  - name: StreamWeave Gateway
    orgId: 1
    folder: StreamWeave
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

