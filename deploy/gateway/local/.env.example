# Copy to .env before starting local Gateway Compose.
GATEWAY_SERVICE_TOKEN=replace-with-a-development-service-token
GATEWAY_ORDERS_URL=http://host.docker.internal:8082
GATEWAY_GRAFANA_ADMIN_USER=admin
GATEWAY_GRAFANA_ADMIN_PASSWORD=replace-with-a-local-grafana-password

