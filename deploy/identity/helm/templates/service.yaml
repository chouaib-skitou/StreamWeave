apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "identity.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
